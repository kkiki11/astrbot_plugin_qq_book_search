# 更新日志

## 0.2.0 — 空结果识别、TXT 限定、每日额度与自动撤回

**面向 QQ 群的文案不再暴露底层服务名称。** 所有提示、错误与诊断输出均已改写为中性表述，例如「服务繁忙，请稍后再试」「账号凭据无效，请联系管理员」。

- **空结果立即返回**：识别服务返回的「没有检索到结果，请尝试其他关键词或筛选条件」等明确空结果，立即回复「未找到相关书籍，请换个关键词试试」，不再等满超时时长。等待超时同样按空结果处理。
- **仅回传 TXT**：`txt_only` 默认开启，列表解析阶段过滤 EPUB / MOBI / AZW3 / PDF；无法识别格式的条目仍保留，下载时再次校验。修复了此前把非 TXT 文件改名成 `.txt` 回传的缺陷。
- **每日下载额度**：`daily_download_limit` 默认 50 本，按自然日自动重置，额度在文件成功回传后扣除；`/找书状态` 可查看剩余额度。
- **完成后自动撤回**：`auto_recall_messages` 默认开启。文件成功回传后，撤回用户命令、提示语与结果列表，群里只留文件。撤回依赖协议端 message_id，因此插件自行调用 `send_group_msg` 记录 ID。
- **结果编号连续**：成人内容过滤会导致原始编号跳号（如 01、04、05…），现按可见结果重排为连续序号，内部保留原编号用于点击。
- **并发串行化**：书籍服务是对话式的，多人同时查询会互相污染结果，现用全局锁串行化与服务的交互。
- **延迟清理临时文件**：下载完成后延迟 180 秒删除，避免与协议端异步读取竞争导致发送失败。
- **新增配置项**：`txt_only`、`adult_filter_threshold`、`daily_download_limit`、`auto_recall_messages`。
- 移除未使用的 `os`、`hashlib` 导入。

## 0.1.7 — 用户关键词清单与简化输出

将用户提供的关键词清单预置到 `adult_filter_keywords`，并保留管理面板的可编辑能力。单字关键词仅在标签边界出现时才匹配，减少对普通书名的误伤。QQ 搜索结果不再显示成人内容隐藏数量或过滤提示，仅展示通过过滤且可下载的书籍。

## 0.1.6 — `🔞` 明确分级硬过滤

将搜索列表中的 `🔞` 内容分级标记设为最高优先级硬过滤条件：即使关闭普通关键词过滤或提高风险阈值，带该标记的结果仍不会显示、保存为可选项或下载。补充该行为的回归测试。

## 0.1.5 — 最新排序、信息展示与成人内容过滤

搜索结果返回后自动点击“最新”排序按钮。QQ 列表增加格式、文件大小与字数展示，例如 `TXT · 5MB · 136.6万字`。默认启用成人与 18+ 内容关键词过滤，命中条目不会显示或被选择；可通过配置开关关闭或维护关键词列表。

## 0.1.4 — 分层书籍选择与下载

根据服务的实际界面调整为两阶段回调：列表顶部 `1–4` 只用于翻页，插件选择底部书籍编号按钮；收到书籍详情后自动点击详情卡片的“下载”按钮，最后等待 TXT 文件回传 QQ。支持下载器在详情消息相同 ID 中编辑并附加 TXT 文件。

## 0.1.3 — 内联按钮选择修复

QQ 群发送序号后，插件现在严格选择搜索结果消息中的对应内联回调按钮，并传入该按钮的 callback data；不再把序号作为普通文本消息发送。若找不到相应内联按钮，会提示重新搜索，避免触发错误的二次检索。

## 0.1.0 — 独立 QQ 群找书插件

将 QQ 群小说搜索、序号选择、TXT 下载和 QQ 文件回传拆分为独立插件 `astrbot_plugin_qq_book_search`，可与 `astrbot_plugin_qq_to_telegram_forwarder` 并存安装。

默认向 `@sobook` 搜索；使用 `book_source_group_ids` 隔离允许使用找书功能的 QQ 群；启动时可复用原转发插件的 `user_session.session` 或 `telegram_user.session`，无需重复登录。搜索列表按 QQ 群与 QQ 用户隔离，并在 15 分钟后过期；TXT 下载完成后清理临时文件。
